# Run Stellar-Jira watchdog periodically (boot + every 90s).

[Unit]
Description=Periodically run stellar-jira automation watchdog

[Timer]
OnBootSec=3min
OnUnitActiveSec=90s
AccuracySec=30s
Persistent=true
Unit=ticket-api-stellar-jira-watchdog.service

[Install]
WantedBy=timers.target
